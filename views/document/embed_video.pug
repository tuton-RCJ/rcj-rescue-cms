doctype html
html
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible")
    title RCJ CMS Video Player
    script(type='text/javascript' src='https://cdn.jsdelivr.net/npm/@clappr/player@latest/dist/clappr.min.js')

  body(style="margin:0;")
    #player(style="height:100vh;")
    script.
        var player = new Clappr.Player({source: "#{video}", parentId: "#player", width: '100%', height: '100%'});
